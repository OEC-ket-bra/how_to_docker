services:
  jupyter:
    image: OEC-Lab/jupyter:1.0
    container_name: 'OEC-Lab-jupyter'
    environment:
      - TZ= Asia/Tokyo
    build: ./jupyter
    working_dir: '/home/jovyan/work'
    volumes:
      - ../jupyter:/home/jovyan/work
    stdin_open: true
    tty: true